# Drawnix Store 浏览器运行镜像：静态产物必须由 build:drawnix-store-images 预先生成。
FROM nginx:1.27-alpine

COPY apps/drawnixstore-web/nginx.conf /etc/nginx/conf.d/default.conf
COPY dist/apps/drawnixstore-web /usr/share/nginx/html

EXPOSE 80
