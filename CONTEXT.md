# Drawnix Store Context

Drawnix Store 管理用户私有的 Drawnix Canvas，并通过业务 API 持久化其可编辑快照与公开预览。此词汇表约束 Canvas 保存和恢复中的业务语义。

## Canvas Persistence

**Canvas 写入预算**:
单次 Canvas 保存请求可承载的完整内容范围；它涵盖快照、预览和保存元数据，而不是某一个字段的长度。
_Avoid_: 单字段请求上限、无限画布大小

**确定性保存失败**:
由当前请求内容、权限或修订状态决定，重复同一请求不会成功的 Canvas 保存失败。待保存快照必须保留，但不得自动重试。
_Avoid_: 离线失败、临时保存失败

**临时保存失败**:
由网络中断、服务端暂时不可用或限流造成，后续相同 Canvas 保存请求可能成功的失败。
_Avoid_: 确定性保存失败

**待保存副本**:
尚未获得服务端成功确认的最新 Canvas 内容；它代表优先于上次已保存内容恢复的用户编辑。
_Avoid_: 缓存、历史版本
